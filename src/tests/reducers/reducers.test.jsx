import * as reducers from 'reducers'
import expect from 'expect'
describe('Reducers', () => {
  //this is used by the store so I am keeping this test
  //check that reducers exist
  it('should exist', () => {
    expect(reducers).toExist();
  });
})
