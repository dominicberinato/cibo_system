import expect from 'expect'

import {Marketing} from 'Marketing'

describe('Marketing', () => {
  it('should exist', () => {
    expect(Marketing).toExist();
  });

  it('should generate reports based on Nationality Breakdown', () => {
    expect('nation').toEqual('lity');
  });
  it('should generate reports based on Area Report', () => {
    expect('nation').toEqual('lity');
  });
  it('should generate reports based on how clients heard about us', () => {
    expect('nation').toEqual('lity');
  });
})
