import React from 'react'
import sinon from 'sinon'
import expect from 'expect'
//use enzyme to test components
import {mount} from 'enzyme'


import {PermComponent} from 'PermComponent'


describe.only('PermComponent',() => {
  it('should exist', () => {
    expect(PermComponent).toExist();
  });

  it('should show list of user properties')

  it('should code form if properties are empty')
})
