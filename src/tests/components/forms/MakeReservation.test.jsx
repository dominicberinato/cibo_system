import expect from 'expect'
import {MakeReservation} from 'MakeReservation'

describe('<MakeReservation/>', () =>{
  it('should exist', () => {
    expect(MakeReservation).toExist();
  })

  it('should allow input of guest name',() => {
    expect(9).toEqual(1);
  })
  it('should allow input of number of guests', () => {
    expect(9).toEqual(1);
  })
  it('should allow input of guest contacts', () => {
    expect(9).toEqual(1);
  })
  it('should allow prepayment', () => {
    expect(9).toEqual(1);
  })
  it('should allow input of dietary restrictions', () => {
    expect(9).toEqual(1);
  })
  it('should allow selection of table', () => {
    expect(9).toEqual(1);
  })
});
